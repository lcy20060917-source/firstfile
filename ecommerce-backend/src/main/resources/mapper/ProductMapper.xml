<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.repository.ProductRepository">

    <resultMap id="productMap" type="com.ecommerce.domain.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="stock" column="stock"/>
        <result property="categoryId" column="category_id"/>
        <result property="imageUrl" column="image_url"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" resultMap="productMap">
        SELECT * FROM t_product WHERE id = #{id}
    </select>

    <select id="search" resultMap="productMap">
        SELECT * FROM t_product
        WHERE status = 1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countSearch" resultType="int">
        SELECT COUNT(*) FROM t_product
        WHERE status = 1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </select>

    <select id="findByCategoryId" resultMap="productMap">
        SELECT * FROM t_product WHERE category_id = #{categoryId} AND status = 1
    </select>

    <insert id="insert" parameterType="com.ecommerce.domain.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_product (name, description, price, stock, category_id, image_url, status)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{categoryId}, #{imageUrl}, #{status})
    </insert>

    <update id="update" parameterType="com.ecommerce.domain.Product">
        UPDATE t_product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStock">
        UPDATE t_product SET stock = #{stock}, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_product WHERE id = #{id}
    </delete>

</mapper>
