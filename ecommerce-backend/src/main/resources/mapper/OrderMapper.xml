<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.repository.OrderRepository">

    <resultMap id="orderMap" type="com.ecommerce.domain.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="receiverAddress" column="receiver_address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <resultMap id="orderItemMap" type="com.ecommerce.domain.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="productPrice" column="product_price"/>
        <result property="quantity" column="quantity"/>
        <result property="totalPrice" column="total_price"/>
    </resultMap>

    <insert id="insertOrder" parameterType="com.ecommerce.domain.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_order (order_no, user_id, total_amount, status, receiver_name, receiver_phone, receiver_address)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress})
    </insert>

    <insert id="insertOrderItems" parameterType="list">
        INSERT INTO t_order_item (order_id, product_id, product_name, product_price, quantity, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productPrice}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>

    <select id="findByUserId" resultMap="orderMap">
        SELECT * FROM t_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM t_order WHERE user_id = #{userId}
    </select>

    <select id="findById" resultMap="orderMap">
        SELECT * FROM t_order WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="orderMap">
        SELECT * FROM t_order WHERE order_no = #{orderNo}
    </select>

    <select id="findItemsByOrderId" resultMap="orderItemMap">
        SELECT * FROM t_order_item WHERE order_id = #{orderId}
    </select>

    <update id="updateStatus">
        UPDATE t_order SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_order WHERE id = #{id}
    </delete>

</mapper>
