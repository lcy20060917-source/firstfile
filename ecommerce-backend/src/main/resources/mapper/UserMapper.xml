<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.repository.UserRepository">

    <resultMap id="userMap" type="com.ecommerce.domain.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="userMap">
        SELECT * FROM t_user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="userMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.ecommerce.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, email, phone, role, status)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.ecommerce.domain.User">
        UPDATE t_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

</mapper>
